// Tab entry for ISBN search, barcode scanning, and add-to-library flow.
import SearchScreen from "@/src/features/search/search.screen";

export default function Search() {
  return (
    <SearchScreen />
  );
}