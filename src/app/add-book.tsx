// Expo route wrapper that renders the manual Add Book screen.
import AddBookScreen from "@/src/features/add-book/add-book.screen";

export default function AddBook() {
  return (
    <AddBookScreen />
  );
}